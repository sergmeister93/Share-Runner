# vendor/claude-plugins/ (staging)

Where vendored Claude Code plugins would live if the project ever needs to bundle one. Empty by
design. **Ponytail is NOT vendored here** — it is a personal/global plugin (see
`docs/skills/PONYTAIL_SETUP.md`). Vendoring any plugin requires inspection + Manager + human approval.
