# vendor/ (third-party staging)

Staging for third-party packages we deliberately vendor. **Owning lane: skills-plugins / manager.**
Nothing is auto-loaded from here. Add a package only after inspection + Manager approval (+ human
approval for executable components). See `docs/skills/SKILL_STRATEGY.md`.
