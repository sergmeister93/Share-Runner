# tests/ (scaffold only)

Future unit tests and browser-verification (Playwright). **Owning lane: qa-verification.** Test code
comes later via Manager work orders. During the setup pass, verification is the validators in
`scripts/` and `comms/bin/`.
