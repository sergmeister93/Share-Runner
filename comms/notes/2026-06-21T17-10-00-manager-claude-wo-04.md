---
ts: 2026-06-21T17:10:00Z
agent: manager-claude
type: work-order
task_id: wo-04-singletons
status: open
owner: backend-claude
target_agent: backend-claude
lane: backend-engine
related:
  - specs/contracts/event_bus_contract.md
  - specs/contracts/game_state_contract.md
  - docs/architecture/SHARE_RUNNER_ARCHITECTURE_SPRINT_PLAN.md
---

## WO-04 — Shared singletons: EventBus, GameState, Constants (SEAM / serial)

**Goal.** Implement the three shared singletons exactly to the frozen seam. This unblocks the whole
fan-out (UX intro/menu, asset loading, gameplay). Get the shapes and reset guarantee right — every
later WO depends on these literals.

**Do (lane: `src/core/**`).**
- `EventBus` — singleton over Phaser's emitter (or a thin `Phaser.Events.EventEmitter`). Centralize
  the event-name string constants here (or in a `Constants`/event-key table) so no executor types
  raw event strings. Names + payload shapes per `specs/contracts/event_bus_contract.md` (the FROZEN
  §4.1 table). Provide typed `emit`/`on`/`off`; ensure listeners can be torn down for restart-safety.
- `GameState` — singleton holding the FROZEN §4.2 shape (`specs/contracts/game_state_contract.md`).
  Implement `reset()` per the guarantee: new `runId`, reset run/score/shares/player/flag/score-flash,
  restore `totalSharesRequired=5` and `levelId='baltimore-waterfront'`, **persist `audio.muted`**
  (E-05), leave no stale timers/tweens/listeners, emit `game:reset-complete`.
- `Constants` — values from architecture plan §4.3 (world facts, asset dims, scene keys, tunables)
  **plus** the provisional physics from the reconcile note: `GRAVITY_Y=3000`,
  `PLAYER_JUMP_VELOCITY=-1300`, `PLAYER_MAX_RUN_SPEED=520`, `PLAYER_ACCELERATION=2600`,
  `COYOTE_TIME_MS=100`, `JUMP_BUFFER_MS=120`, `SHARE_SCORE_VALUE=100`, `TOTAL_SHARES_REQUIRED=5`,
  etc. Mark the tunables `[TUNABLE]` in comments. `levelId='baltimore-waterfront'`.
- Refactor `src/core/PhaserGameConfig.ts` to import `WORLD_WIDTH`/`WORLD_HEIGHT` from `Constants`
  (remove the duplicated literals it flagged in WO-03).
- Wire `app:boot { timestampMs }` emit at game boot now that EventBus exists (the WO-03 TODO).

**Acceptance (record in your `result`).**
- `npm run typecheck` exit 0; `npm run build` exit 0.
- A tiny self-check proves `reset()` clears a dirtied state and **keeps `muted`** (a `node`/`tsx`
  assert script or a `tests/`-free inline `__check__` is fine — ponytail: smallest runnable check).
- No raw event-name strings outside the EventBus/Constants table; `baltimore-waterfront` everywhere
  (no superseded underscore id).
- Stay in `src/core/**` (+ the PhaserGameConfig refactor). Do not build scenes/objects/systems yet.

Commit code + your `result` ledger line together, status `review`, then stop. I verify + merge,
then fan out: WO-05 (asset adapter) to you, WO-07 (Intro) to ux-codex.
