---
ts: 2026-06-21T21:56:00Z
agent: manager-claude
type: work-order
task_id: wo-18-pixel-camera-reachability
status: open
owner: qa-agent
target_agent: qa-agent
lane: qa-verification
related:
  - assets/levels/baltimore/metadata/baltimore_level_manifest.json
  - src/scenes/LevelScene.ts
---

## WO-18 — Pixel-art crispness + camera + 5-coin reachability (E-08 gate)

**Goal.** Confirm the visual contract and the §7 layout's playability. Pairs with WO-17; can run on
the same recorded session.

**Verify:**
- **Pixel-art crispness:** no blur/smoothing on the skyline, rowhomes, platform bricks, or player
  sprite. `pixelArt:true`/`roundPixels:true`/`antialias:false` + CSS `image-rendering:pixelated` are
  set — confirm the rendered output is crisp at the contain-fit scale. Screenshot evidence.
- **Contain-fit / scale:** canvas letterboxes correctly (aspect ~1.764) at a few window sizes; no
  stretching.
- **Camera:** currently the camera shows the **whole 4400×2494 world** (contain-fit at game size =
  world size), so the player renders small and there is no side-scroll. Assess whether that is
  acceptable for first playable or whether a follow-cam / zoom is warranted; record a recommendation
  (this is a tuning call, not a contract change — Manager decides any change).
- **Reachability (E-08 content gate):** with the provisional physics (GRAVITY_Y 3000, JUMP_VELOCITY
  -1300, MAX_RUN_SPEED 520, ACCEL 2600), confirm **all 5 shares are actually reachable** and the flag
  is reachable by intentional (non-pixel-perfect) play — especially the moving-platform coins
  (share_02/03/05). If any coin/flag is unreachable, record exact ids + the gap; Manager retunes
  physics (E-07) or adjusts placement (re-opens WO-02).

**Exemptions.** The flag is a **marked primitive placeholder** (no flag sprite exists; art-gen is
out of scope / human-gated) — exempt it from pixel-art crispness; just confirm its hitbox works.

**Acceptance.** Recorded findings (screenshots + notes) on crispness, contain-fit, camera, and
per-coin reachability. Write under `docs/qa/`.

**Process.** qa lane; commit your own files + ledger, status `review`, stop. Same Phaser pause-on-
hidden gotcha as WO-17 applies for automated runs.
