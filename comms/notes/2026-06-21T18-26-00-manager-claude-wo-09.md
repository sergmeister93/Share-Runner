---
ts: 2026-06-21T18:26:00Z
agent: manager-claude
type: work-order
task_id: wo-09-level-world
status: open
owner: backend-claude
target_agent: backend-claude
lane: backend-engine
related:
  - src/core/Constants.ts
  - src/systems/AssetCatalog.ts
  - specs/level/baltimore_level_contract.md
---

## WO-09 — Level world scaffold as backend systems (parallel now)

**Goal.** The backend world pieces the Level scene will assemble: world bounds, the single ground
static body, and camera containment. Build these as **backend-lane systems/objects** — do NOT create
`src/scenes/LevelScene.ts` (that's the ux/Manager integration seam, WO-16). This is independent of the
Preloader: it needs only Constants + placement metadata, not loaded textures, so it runs in parallel
with WO-06/WO-07.

**Do (lane: `src/systems/**`, `src/objects/**`).**
- `LevelWorld` system (or `buildLevelWorld(scene, catalog)`): sets `scene.physics.world.setBounds`
  and `scene.cameras.main.setBounds` to `WORLD_WIDTH × WORLD_HEIGHT` (Constants).
- Ground: one static body, top at `GROUND_TOP_Y` (1905), width `GROUND_WIDTH` (4400) — a single body,
  not segmented (level contract). Expose it for player/platform collision wiring.
- `CameraSystem` helper: containment + a `followTarget`/establishing-pan hook (emit
  `camera:establishing-pan:start/complete` per the frozen events; pan duration `CAMERA_PAN_DURATION_MS`).
  Player-follow wiring lands when the Player exists (WO-10).
- Keep these Phaser-typed but factor pure geometry (bounds math) so a node check can assert it.

**Acceptance (record in your `result`).**
- `npm run typecheck` + `npm run build` exit 0.
- A node self-check (sibling `check:*`) asserts world bounds = 4400×2494 and ground body =
  {topY:1905, width:4400, single body}. Camera containment is browser-verified later at integration
  (WO-16) — note that as `not-run (deferred to WO-16)` with the reason.
- No `src/scenes/**` edits; no loaded-texture dependency; uses Constants + AssetCatalog metadata only.

**Process.** Lane-only; **commit your own** lane files + ledger in one commit, status `review`, stop.
Do NOT `git add -A` (ux-codex is in `src/scenes`/`src/ui` in parallel). After this merges you get
WO-10 (Player) → then WO-11 platforms / WO-12 coins / WO-13 flag.
