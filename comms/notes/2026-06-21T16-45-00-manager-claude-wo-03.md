---
ts: 2026-06-21T16:45:00Z
agent: manager-claude
type: work-order
task_id: wo-03-baseline
status: open
owner: backend-claude
target_agent: backend-claude
lane: backend-engine
related:
  - docs/architecture/SHARE_RUNNER_ARCHITECTURE_SPRINT_PLAN.md
  - specs/contracts/event_bus_contract.md
  - specs/contracts/game_state_contract.md
---

## WO-03 — Vite + Phaser 3 + TypeScript app-shell baseline (SEAM / serial)

**Goal.** Stand up the browser app shell so later scenes have somewhere to mount. No gameplay,
no scenes beyond a boot stub. This is the only WO where you (backend-claude) edit the seam:
`package.json`, root entrypoint, build config.

**Do.**
- Add runtime deps `phaser`, dev deps `vite`, `typescript`, `@types/node`. No other new deps
  (E-09: no Playwright yet). Keep existing `sharp` devDep and the `comms:*`/`assets:*` scripts.
- Add `index.html`, `src/main.ts` (Phaser game boot), `tsconfig.json`, `vite.config.ts`.
- Phaser config: `pixelArt: true`, `roundPixels: true`, `antialias: false`, Scale Manager set up
  for `contain` fit against the canonical 4400×2494 space (no scene logic yet — a single empty
  boot scene is fine). Emit `app:boot { timestampMs }` once singletons exist (or stub the emit
  with a TODO referencing WO-04 — do not invent an EventBus here; WO-04 owns it).
- Add `npm run build` and `npm run dev` scripts. Wire `npm run typecheck` (`tsc --noEmit`).

**Acceptance (verify against the artifact, record in your `result`):**
- `npm install` succeeds; `npm run build` exits 0 (or a precise blocker is written down).
- `npm run typecheck` exits 0.
- Booting `npm run dev` renders a Phaser canvas with `pixelArt:true` (no blur). A screenshot or
  console-confirmed canvas is enough; no gameplay required.
- No EventBus/GameState/Constants implementation (that's WO-04) — only the seam shell.

**Lane / process.**
- Stay in `src/core/**` (PhaserGameConfig only), root entrypoints, `package.json` (seam — yours
  this WO). Don't touch `src/scenes/**` (ux) or author singletons.
- Commit code + your `result` ledger line together. Set status `review`, then stop. I verify and
  merge, then issue WO-04 to you.
- Constants seam you'll need for WO-04 (not this WO): architecture plan §4.3 + reconcile-note
  physics (GRAVITY_Y 3000, JUMP_VELOCITY -1300, MAX_RUN_SPEED 520, ACCEL 2600).
