---
ts: 2026-06-21T18:35:00Z
agent: manager-claude
type: work-order
task_id: wo-08-menu
status: open
owner: ux-codex
target_agent: ux-codex
lane: ux-frontend
related:
  - src/scenes/IntroScene.ts
  - src/scenes/MenuStubScene.ts
  - specs/contracts/event_bus_contract.md
---

## WO-08 — Menu scene (do after WO-06)

**Goal.** Replace the temporary `MenuStubScene` with the real menu: exactly two options,
`Start Game` and `Quit`. Sequence: take this after you submit WO-06.

**Do (lane: `src/scenes/**`, `src/ui/**`).**
- `MenuScene` keyed `SCENE_MENU` (`'Menu'`). **Remove `MenuStubScene`** and update
  `src/scenes/index.ts` `UX_BOOT_SCENES` to export `MenuScene` in its place (Intro stays first). The
  manager-owned `src/main.ts` already registers `UX_BOOT_SCENES`, so no main.ts change is needed —
  if you find one is, raise a `handoff` and I'll wire it.
- On show, emit `menu:shown { options: ['Start Game','Quit'] }`.
- `Start Game` → emit `game:start-requested { source:'menu' }` then `this.scene.start(SCENE_PRELOADER)`.
- `Quit` → emit `game:quit-requested { source:'menu' }`. Browser-safe per **E-04**: return to
  title/menu (or attempt window close then fall back) and, if the close is blocked, emit
  `game:quit-fallback-shown { reason:'browser-blocked-window-close' }` and show a small fallback
  message. No `process.exit`, no unguarded `window.close()` assumptions.
- Keyboard + pointer selection; pixel-crisp; restart-safe teardown in `shutdown()`.

**Acceptance.**
- `npm run typecheck` + `npm run build` exit 0.
- Browser-verify: menu shows exactly the two options; Start transitions toward Preloader; Quit shows
  the safe fallback. Screenshot/console evidence in your `result`.
- Only the two options; only frozen event names; no gameplay state invented.

**Process.** Lane-only; **commit your own** lane files + ledger in one commit, status `review`, stop.
Do NOT `git add -A`.
