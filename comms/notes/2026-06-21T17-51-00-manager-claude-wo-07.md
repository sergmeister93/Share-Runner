---
ts: 2026-06-21T17:51:00Z
agent: manager-claude
type: work-order
task_id: wo-07-intro-scene
status: open
owner: ux-codex
target_agent: ux-codex
lane: ux-frontend
related:
  - specs/contracts/event_bus_contract.md
  - src/core/EventBus.ts
  - src/core/Constants.ts
---

## WO-07 — Intro scene + arcade title spectacle (parallel after seam)

**Goal.** The first scene the player sees: a 90s/early-2000s arcade "Share-Runner" title beat, then
hand off to Menu. Presentation only — no gameplay state beyond scene-local.

**The seam is ready (WO-04 merged).** Import from `src/core/`:
- `eventBus` (`src/core/EventBus.ts`) — typed; the only valid event keys are in `EventPayloads`.
- Scene keys + tunables from `src/core/Constants.ts` (`SCENE_INTRO`, `SCENE_MENU`,
  `CAMERA_PAN_DURATION_MS`, etc.). Never type raw event strings.

**Do (lane: `src/scenes/**`, `src/ui/**`, `src/styles/**`).**
- `IntroScene` (key `SCENE_INTRO` = `'Intro'`). On enter, emit `intro:start { runId }` (use
  `gameState.runId` from `src/core/GameState.ts`).
- Title spectacle: emit `intro:title-slam { title:'Share-Runner', intensity }` on the main beat and
  `intro:scanline-pulse { pulseIndex, totalPulses }` for pulse beats. Make it feel arcade — big
  pixel/monospace title, scanline/CRT vibe — but keep pixel-art crisp (no blur/smoothing; respect
  `pixelArt`). Use the `frontend-design` skill for the look.
- On completion (timer or key/click to skip), emit `intro:complete { nextScene:'Menu' }` and
  `this.scene.start(SCENE_MENU)`. Tear down listeners/tweens in `shutdown()` (restart-safety).
- Register `IntroScene` in the boot list. Coordinate with me before editing `src/main.ts` (seam):
  if you need the scene list wired, say so in a `handoff` and I'll wire it, OR add a small
  `src/scenes/index.ts` your scene exports from and I wire main.ts once. **Do not edit `src/core/**`
  or `package.json`** (not your lane).

**Acceptance (record in your `result`).**
- `npm run typecheck` + `npm run build` exit 0.
- Booting the app shows the Intro first, plays the title beat, and transitions to a (stub is fine for
  now) Menu — or, if Menu isn't built yet (WO-08), transitions cleanly to `SCENE_MENU` and logs/asserts
  the `intro:complete` emit. Browser-verify (screenshot or console-confirmed) that the title is crisp.
- Only frozen event names used; no gameplay state invented.

**Process.** Stay in your lane. **Commit your own work**: `git add` only your lane files + the ledger,
`result` line in the SAME commit (KERNEL invariant 3), status `review`, then stop. Do NOT `git add -A`
(backend-claude is working WO-05 in parallel). Flag any seam need (main.ts scene wiring) via `handoff`.
