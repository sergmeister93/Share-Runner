---
ts: 2026-06-21T18:55:00Z
agent: manager-claude
type: work-order
task_id: wo-10-player
status: open
owner: backend-claude
target_agent: backend-claude
lane: backend-engine
related:
  - src/systems/LevelWorld.ts
  - src/systems/AssetCatalog.ts
  - src/core/Constants.ts
---

## WO-10 — Player object + controller (parallel after world scaffold)

**Goal.** The player: spawns on the left, runs and jumps with the provisional physics, animates from
the 160×160 male_hero frames, and lands reliably on the ground body. Use the **game-feel** and
**2d-collision** skills — coyote time, jump buffering, variable jump height; axis-separated AABB feel;
reliable grounded detection.

**Do (lane: `src/objects/**`, `src/systems/**`).**
- `Player` object (arcade-physics sprite/body). Spawn from the authored placement
  (`catalog.placements.playerSpawn` = x220/y1905, **bottom-center/feet** anchor — convert to body
  position using the 160×160 frame, feet at y=1905).
- Movement/input controller: left/right run with `PLAYER_ACCELERATION`/`PLAYER_MAX_RUN_SPEED`,
  gravity `GRAVITY_Y`, jump `PLAYER_JUMP_VELOCITY`, `COYOTE_TIME_MS`, `JUMP_BUFFER_MS`, variable jump
  (release-to-cut). All from Constants ([TUNABLE]).
- Collide with `LevelWorld.groundZones`. Maintain `movementState` (idle|walk|run|jump|fall|fall-loop)
  and drive animations by the catalog spritesheet keys (`<assetId>-idle` … `-fallLoop`), frame counts
  from the player manifest. Mirror state into `gameState.player` (x/y/velocity/facing/movementState/
  isGrounded). Emit frozen `player:spawned`, `player:jump` (with jumpCount), `player:grounded`
  (surfaceId), `player:fall`.
- Keep input/physics logic factored so a node check can assert the state machine (grounded→jump→fall→
  grounded; coyote/buffer windows) with a mock body. Animation/texture visuals are browser-verified at
  integration (WO-16) — mark that `not-run (WO-16)`.

**Acceptance (record in your `result`).**
- `npm run typecheck` + `npm run build` exit 0; a node self-check (`check:*`) asserts the movement
  state transitions + coyote/buffer timing logic.
- Spawn math puts feet at y=1905; uses Constants for every physics value (no magic numbers); animation
  keys/frame-counts come from the catalog/manifest (no hardcoding).
- No `src/scenes/**` edits (Player is instantiated by the LevelScene at WO-16).

**Process.** Lane-only; **commit your own** lane files + ledger in one commit (KERNEL invariant 3),
status `review`, stop. Do NOT `git add -A` (ux-codex is in `src/scenes`/`src/ui`). Next after this:
WO-11 platforms (+WO-02 placements) → WO-12 coins → WO-13 flag.
