---
ts: 2026-06-21T17:50:00Z
agent: manager-claude
type: work-order
task_id: wo-05-asset-adapter
status: open
owner: backend-claude
target_agent: backend-claude
lane: backend-engine
related:
  - assets/asset_library_manifest.json
  - assets/levels/baltimore/metadata/baltimore_level_manifest.json
  - specs/contracts/asset_loading_contract.md
---

## WO-05 — Manifest-driven asset-loading adapter (parallel after seam)

**Goal.** One adapter that turns the asset manifests into Phaser load instructions and
runtime-queryable metadata. Zero hardcoded paths or dimensions anywhere downstream.

**Do (lane: `src/systems/**`, `src/data/**`).**
- Read `assets/asset_library_manifest.json` first, then the level manifest
  `assets/levels/baltimore/metadata/baltimore_level_manifest.json` and the prefab/sprite manifests
  it points to (player, collectables, environment). Resolve relative paths correctly.
- Expose typed accessors: image/spritesheet/audio entries with `{ key, path, width, height, frames }`
  and the authored placements (`authoredGameplayPlacements`: playerSpawn, flag, environmentObstacles,
  collectables) so WO-09..WO-13 read placement from here — not from prose.
- Provide a `queueLoads(scene.load)` helper the Preloader (WO-06) calls. Do not build the Preloader
  scene (ux lane).
- **Manifest discipline:** never infer a dimension from an image; if the manifest lacks a needed
  field, throw a clear `manifest-mismatch` error naming the missing key. (`asset-manifest-discipline`)

**Acceptance (record in your `result`).**
- `npm run typecheck` + `npm run build` exit 0.
- A small node self-check (extend `check:core` or a sibling `check:assets`) proves the adapter loads
  the real manifests, returns 5 authored collectables + 10 obstacles + spawn(220,1905) + flag(4230),
  and throws on a deliberately missing key. Record the command + exit code.
- No asset path/dimension literals hardcoded; manifests are the only source.

**Process.** Stay in your lane. **Commit your own work**: `git add` only your lane files + the ledger,
write your `result` line in the SAME commit (KERNEL invariant 3), status `review`, then stop. Do NOT
`git add -A` (ux-codex is working WO-07 in parallel — don't sweep its files). I verify + complete.
