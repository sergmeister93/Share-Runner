---
ts: 2026-06-21T21:05:00Z
agent: manager-claude
type: work-order
task_id: wo-14-hud
status: open
owner: ux-codex
target_agent: ux-codex
lane: ux-frontend
related:
  - src/core/EventBus.ts
  - src/core/GameState.ts
  - src/core/Constants.ts
---

## WO-14 — HUD: share counter + score + completion flash (unblocked)

**Goal.** The in-level HUD (share progress + score) and the arcade completion flash. This is the last
executor work order before I do the WO-16 integration.

**The seam is fully ready.** Import `eventBus`/`gameState`/Constants from `src/core/`. All gameplay
events already fire (backend WO-12/WO-13 merged). Build the HUD as a scene-attachable `src/ui` unit;
I instantiate it in the real LevelScene at WO-16.

**Do (lane: `src/ui/**`).**
- `HUD` (camera-fixed, `setScrollFactor(0)`, pixel-crisp `setResolution(1)`): a share counter
  **`X / 5`** and a **score** readout (E-10: show both). Initialize from `gameState`
  (sharesCollected/score/totalSharesRequired).
- Listen and update: `score:changed { score, sharesCollected, totalSharesRequired }`,
  `share:collected` (increment feedback), and `hud:share-counter-pulse { sharesCollected,
  totalSharesRequired }` → pulse/highlight the counter (spectacle).
- **Own the completion score-flash spectacle:** on `level:complete { score, sharesCollected }`, run
  the flash — emit `score:flash:start { score, durationMs: SCORE_FLASH_DURATION_MS }`, animate a
  crisp arcade score/share flash, then emit `score:flash:complete { score }` when done. Set/clear
  `gameState.ui.scoreFlashActive` around it.
- Restart-safe: tear down listeners/tweens in `shutdown()`; re-init cleanly from `gameState` so a
  reset shows `0 / 5`, score 0.

**Acceptance (record in your `result`).**
- `npm run typecheck` + `npm run build` exit 0.
- Browser-verify (attach to a stub/test scene, drive the events): counter advances 0→5 and score by
  100/coin; counter pulses on `hud:share-counter-pulse`; on `level:complete` the flash runs and
  emits `score:flash:start`→`score:flash:complete`. Screenshot/console evidence; pixel-crisp.
- Only frozen event names; reads state from `gameState`/Constants; no gameplay logic invented (HUD
  reflects truth, doesn't compute completion/score).

**Process.** Lane-only (`src/ui/**`); **commit your own** files + ledger in one commit (KERNEL
invariant 3), status `review`, stop. Do NOT `git add -A`.
