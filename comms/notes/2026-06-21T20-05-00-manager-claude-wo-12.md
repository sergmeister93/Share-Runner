---
ts: 2026-06-21T20:05:00Z
agent: manager-claude
type: work-order
task_id: wo-12-coins
status: open
owner: backend-claude
target_agent: backend-claude
lane: backend-engine
related:
  - src/systems/PlatformSystem.ts
  - src/systems/AssetCatalog.ts
  - src/core/GameState.ts
---

## WO-12 — Share coin objects + system (after platforms)

**Goal.** Exactly 5 collectible share coins with bob animation, collect-once, scoring, and the
move-with-platform behavior (E-02). This is the heart of the win condition.

**Do (lane: `src/objects/**`, `src/systems/**`).**
- `ShareCoin` object + `CollectibleSystem` (`buildCollectibles(scene, catalog, platformSystem)`).
  Read the 5 instances from `catalog.placements.collectables` — id, center{x,y}, anchorPlatformId,
  behavior (`static-bob` | `moves-with-platform`). Get the coin texture **key/dims from the catalog**
  (collectables manifest `share` key — do NOT hardcode the key; derive it like AssetCatalog does).
- Bob: gentle vertical sine, `COIN_BOB_AMPLITUDE_PX` / `COIN_BOB_DURATION_MS` (Constants, [TUNABLE]).
- **E-02 move-with-platform:** `share_02`(plat_v_01), `share_03`(plat_h_01), `share_05`(plat_h_02)
  track their anchor platform at the **fixed offset above it** captured at authoring (center.y is
  above the platform top). Read live platform position from `platformSystem` each frame so the coin
  never drifts out of reach. `share_01`/`share_04` are `static-bob`.
- **Collect-once:** overlap with the player collects it exactly once (guard against double-fire).
  On collect: push id to `gameState.collectedShareIds` (ignore if already present), increment
  `gameState.sharesCollected`, add `SHARE_SCORE_VALUE` (100, E-10) to `gameState.score`. Emit
  `share:collected { shareId, collectedCount, totalRequired: 5, scoreDelta: 100 }`, then
  `score:changed { score, sharesCollected, totalSharesRequired: 5 }`, then
  `hud:share-counter-pulse { sharesCollected, totalSharesRequired: 5 }`.
- Expose the coin sprites for `scene.physics.add.overlap(player, coins)`. Provide `update(dtSec)` for
  bob + moving-coin tracking. Restart-safe (rebuilt cleanly on scene restart).

**Acceptance (record in your `result`).**
- `npm run typecheck` + `npm run build` exit 0; a node self-check asserts: exactly 5 coins from
  manifest with unique ids; collecting one increments count + score by 100 and is idempotent on a
  second overlap; moving coins report a position offset above their anchor platform.
- Texture key/dims from catalog (no hardcoded asset key); coords from placements; SHARE_SCORE_VALUE
  from Constants. No `src/scenes/**` edits.

**Process.** Lane-only; **commit your own** lane files + ledger in one commit, status `review`, stop.
Do NOT `git add -A`. Next: WO-13 flag + completion (needs this + WO-09).
