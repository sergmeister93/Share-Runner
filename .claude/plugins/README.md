# .claude/plugins/ (documented staging area)

**Do not assume this auto-loads.** It is a staging/documentation area only. Plugins are enabled
through Claude Code's plugin system (`/plugin`) with a human-approved trust prompt — see
`docs/skills/PLUGIN_SETUP.md`. No executable plugin/hook/MCP/monitor is enabled without Manager +
human approval (`docs/skills/SKILL_STRATEGY.md`).
