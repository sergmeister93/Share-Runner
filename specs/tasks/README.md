# specs/tasks/ (scaffold)

Manager-authored work orders / task specs land here during the implementation pass. **Owning lane:
manager.** A work order is a pointer (goal + acceptance criteria + lane + where to look), never
implementation code (KERNEL §7). Empty this pass.
