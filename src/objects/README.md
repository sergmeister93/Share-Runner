# src/objects/ (scaffold only)

Future game entities: Player, Platform (stationary/vertical/horizontal), ShareCoin, Flag. **Owning
lane: backend-engine.** Runtime implementation comes later via Manager work orders. See
`specs/level/baltimore_level_contract.md`.
