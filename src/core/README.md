# src/core/ (scaffold only)

Future foundation: EventBus, GameState, Constants. **Owning lane: backend-engine.** Runtime
implementation comes later via Manager work orders. See `specs/contracts/event_bus_contract.md` and
`specs/contracts/game_state_contract.md`.
