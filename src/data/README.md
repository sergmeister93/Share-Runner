# src/data/ (scaffold only)

Future data adapters: manifest-driven level/config loading (no hardcoded asset data). **Owning lane:
backend-engine.** Runtime implementation comes later via Manager work orders. See
`specs/contracts/asset_loading_contract.md`.
